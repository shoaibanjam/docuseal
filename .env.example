# Trustseal production environment
# Copy to .env and fill in values: cp .env.example .env

# Required: PostgreSQL (use host.docker.internal when DB runs on same EC2)
DATABASE_URL=postgresql://user:password@host.docker.internal:5432/docuseal

# Required: generate with: openssl rand -hex 64
SECRET_KEY_BASE=

# Required: canonical base URL (no trailing slash). Used for links, emails, and file URLs when
# the request does not supply Host (e.g. background jobs).
#
# Production (HTTPS):     APP_URL=https://trustseal.cybroscloud.com   FORCE_SSL=true
# Docker compose local:   omit APP_URL or use http://localhost:9090   FORCE_SSL=false (compose defaults APP_URL)
# bin/rails s / dev:      APP_URL=http://localhost:3000               FORCE_SSL=false
APP_URL=https://trustseal.cybroscloud.com

# Optional: multitenant / console CDN host pieces (single-tenant Docker often ignores this)
HOST=localhost

# true when the public site is HTTPS (e.g. trustseal.cybroscloud.com). false for local HTTP.
FORCE_SSL=true

# Optional: for email links (defaults to HOST)
# EMAIL_HOST=your-domain.com

# Optional: external Redis (omit to use in-container Redis)
# REDIS_URL=redis://:password@host:6379/0

# Optional: AWS Secrets Manager (replaces SECRET_KEY_BASE and other vars from secret)
# AWS_SECRET_MANAGER_ID=your-secret-id

# Optional: S3 for attachments (omit to use disk storage in volume)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# S3_ATTACHMENTS_BUCKET=

# Optional: skip auto migrations on boot (default: migrations run)
# RUN_MIGRATIONS=false
