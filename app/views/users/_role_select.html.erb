<div class="form-control">
  <%= f.label :role, class: 'label' %>
  <%= f.select :role,
        nil,
        {},
        {
          class: 'base-select w-full',
          title: t('users_placeholder_role_select', default: 'Workspace role for this user'),
          aria: { describedby: 'users-modal-description' }
        } do %>
    <option value="admin"><%= t('admin') %></option>
    <option value="editor" disabled><%= t('editor') %></option>
    <option value="viewer" disabled><%= t('viewer') %></option>
  <% end %>
  <% if Docuseal.multitenant? %>
    <label class="label">
      <span class="label-text-alt">
        <%= t('click_here_to_learn_more_about_user_roles_and_permissions_html') %>
      </span>
    </label>
  <% end %>
</div>
